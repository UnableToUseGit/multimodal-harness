# 数据模式文档模板

## 文档目标

本文档用于说明 `<SchemaName>` 的设计目的、字段定义、语义约束以及它与其他 schema 的关系。本文档面向开发者，目标是帮助读者快速理解该 schema 的职责边界和使用方式。

## Schema 概览

- 名称：`<SchemaName>`
- 主要职责：`<一句话说明该 schema 用来表达什么>`
- 实现形式：`dataclass` / `Pydantic model` / `<其他>`

## 字段定义

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `<field_1>` | `<type>` | 是/否 | `<default>` | `<简要说明>` |
| `<field_2>` | `<type>` | 是/否 | `<default>` | `<简要说明>` |
| `<field_3>` | `<type>` | 是/否 | `<default>` | `<简要说明>` |

## 字段说明

### `<field_1>`

- 语义：`<该字段表示什么>`
- 约束：`<取值范围、是否允许为空、是否要求唯一等>`
- 注意事项：`<容易误解的点>`

### `<field_2>`

- 语义：`<该字段表示什么>`
- 约束：`<取值范围、是否允许为空、是否要求唯一等>`
- 注意事项：`<容易误解的点>`

### `<field_3>`

- 语义：`<该字段表示什么>`
- 约束：`<取值范围、是否允许为空、是否要求唯一等>`
- 注意事项：`<容易误解的点>`

## 校验规则与约束

- `<规则 1：例如字段之间的逻辑关系>`
- `<规则 2：例如某些字段不能为空>`
- `<规则 3：例如某些值必须由上游保证>`


## 示例

```python
<SchemaName>(
    <field_1>=...,
    <field_2>=...,
    <field_3>=...,
)
```
