# Design

Use this file for high-level design constraints that span multiple modules.

Current direction:

- keep the repo video-only
- keep generated outputs human-readable
- prefer stable schemas over implicit prompt assumptions
