# Atlas 目录格式文档模板

## 文档目标

本文档用于说明 `<AtlasName>` 的目录结构、文件组织方式和外部表示约定。本文档面向开发者、评审者和下游消费者，目标是帮助读者理解该 atlas 在落盘后应呈现为什么样的目录形式，以及哪些内容属于稳定契约。

## 适用对象

- atlas 类型：`<Canonical / Derived / 其他>`
- 输出目标：`<该 atlas 的最终目录表示面向谁>`
- 维护模块：`<通常是 persistence 或对应 workflow>`

## 根目录结构

```text
<atlas-root>/
├── <file_or_dir_1>
├── <file_or_dir_2>
└── <file_or_dir_3>
```

## 根目录项说明

### `<root_item_1>`

- 类型：`file / directory`
- 作用：`<该项的用途>`
- 是否必需：`是 / 否`
- 说明：
  - `<补充说明 1>`
  - `<补充说明 2>`

### `<root_item_2>`

- 类型：`file / directory`
- 作用：`<该项的用途>`
- 是否必需：`是 / 否`
- 说明：
  - `<补充说明 1>`
  - `<补充说明 2>`

## 子目录与文件说明

### `<subdir_or_group_1>`

- 目录结构：

```text
<subdir_or_group_1>/
├── <child_1>
├── <child_2>
└── <child_3>
```

- 作用：`<该子目录承载什么内容>`
- 文件约定：
  - `<file_1>`：`<作用>`
  - `<file_2>`：`<作用>`
  - `<file_3>`：`<作用>`

## 必需项与可选项

### 必需项

- `<必需项 1>`
- `<必需项 2>`
- `<必需项 3>`

### 可选项

- `<可选项 1>`
- `<可选项 2>`
- `<可选项 3>`

## 文件内容约定

### `<file_name_1>`

- 内容类型：`Markdown / JSON / 媒体文件 / 其他`
- 主要字段或内容：
  - `<字段 1>`
  - `<字段 2>`
  - `<字段 3>`
- 说明：
  - `<该文件的格式要求>`
  - `<该文件与其他文件的关系>`

### `<file_name_2>`

- 内容类型：`Markdown / JSON / 媒体文件 / 其他`
- 主要字段或内容：
  - `<字段 1>`
  - `<字段 2>`
  - `<字段 3>`
- 说明：
  - `<该文件的格式要求>`
  - `<该文件与其他文件的关系>`

## 稳定契约

- `<哪些目录项或文件格式应被视为稳定契约>`
- `<哪些字段或命名方式不应静默变更>`
- `<哪些内容可在保持兼容前提下扩展>`

## 示例目录树

```text
<atlas-root>/
├── <example_1>
├── <example_2>
└── <example_3>
```
